// click event =  its similar to HTML's onClick event handler and we will learn how React handles a callback to the event handler

import Button from './Button.jsx';
import ProfilePicture from './ProfilePicture.jsx'


function App() {

  return(
  <ProfilePicture/>
  );
}

export default App;
